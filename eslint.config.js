import js from "@eslint/js";
import globals from "globals";
import reactHooks from "eslint-plugin-react-hooks";
import reactRefresh from "eslint-plugin-react-refresh";
import tseslint from "typescript-eslint";

export default tseslint.config(
  {
    // The MCP edge function bundle is auto-generated by the @lovable.dev/mcp-js
    // Vite plugin from src/lib/mcp/**. It gets rewritten on every build, so we
    // exclude only this file; other Supabase edge functions must remain linted.
    ignores: ["dist", "supabase/functions/mcp/index.ts"],
  },
  {
    extends: [js.configs.recommended, ...tseslint.configs.recommended],
    files: ["**/*.{ts,tsx}"],
    languageOptions: {
      ecmaVersion: 2020,
      globals: globals.browser,
    },
    plugins: {
      "react-hooks": reactHooks,
      "react-refresh": reactRefresh,
    },
    rules: {
      ...reactHooks.configs.recommended.rules,
      "react-refresh/only-export-components": ["warn", { allowConstantExport: true }],
      "@typescript-eslint/no-unused-vars": "off",
    },
  },
  {
    // As tabelas ELO entram por migration e ainda não existem no arquivo de tipos
    // gerado pelo Supabase. O cast fica isolado nesta página até a regeneração.
    files: ["src/pages/Elo.tsx"],
    rules: {
      "@typescript-eslint/no-explicit-any": "off",
    },
  },
);
